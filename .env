# uxixfhthdgwxqfltvudt is the ONLY correct Supabase project for this app.
# It has happened TWICE (Lovable's own auto-commit bot, gpt-engineer-app,
# silently overwriting this file) that these values got swapped to a
# different, inaccessible project (uxixfhthdgwxqfltvudt), which broke the
# live site both times because Vite bakes these values into the production
# bundle at build time. If you ever see anything other than
# uxixfhthdgwxqfltvudt below, that is this regression recurring — revert it,
# do not treat it as an intentional update. CI (guard-supabase-project.yml)
# fails the build if this drifts, but that check cannot stop it from
# happening — only catch it loudly once it does.
VITE_SUPABASE_PROJECT_ID="uxixfhthdgwxqfltvudt"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_8E0mnb2b-qvUSffU_1nuBg_hIY04YcA"
VITE_SUPABASE_URL="https://uxixfhthdgwxqfltvudt.supabase.co"
